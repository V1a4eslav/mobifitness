// const icons = document.querySelectorAll('[data-icon]');
// document.addEventListener('mousemove', paralaxIcon);
// function paralaxIcon(e) {
//    icons.forEach(icon => {
//       const speed = icon.getAttribute('data-iconspeed');
//       const x = (window.innerWidth - e.pageX * speed) / 1000;
//       const y = (window.innerHeight - e.pageY * speed) / 1000;

//       icon.style.transform = `translateX(${x}px) translateY(${y}px)`
//    });
// }